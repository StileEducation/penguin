{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Ruby Tests",
      "type": "ruby_lsp",
      "request": "launch",
      "program": "${workspaceFolder}/test/${relativeFile}",
      "env": {
        "BUNDLE_GEMFILE": "${workspaceFolder}/Gemfile",
        "RUST_LOG": "debug"
      }
    }
  ]
}
